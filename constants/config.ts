import { Dimensions } from "react-native";

export const GRID_SIZE = 6;

export const animalEmojis = ["🐶", "🐱", "🐭", "🐰", "🦊", "🐻"];

export const TILE_SIZE = Dimensions.get("window").width / GRID_SIZE;

export const TASK_GOAL = 10;
